# Integrations
In the root of this folder is integration flows as .json files which can be imported into iHub.
If they require XSD schema to parse the response then these are available in the xsd-schema folder
